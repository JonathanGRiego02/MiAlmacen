<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icono_camion"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icono_camion"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiAlmacen"
        tools:targetApi="31">
        <activity
            android:name=".actividades.AlmacenesActivity"
            android:exported="false"
            android:label="@string/title_activity_almacenes"
            android:theme="@style/Theme.MiAlmacen.NoActionBar" />
        <activity
            android:name=".actividades.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".actividades.MainActivity"
            android:exported="true"></activity>

        <activity android:name=".actividades.AjustesActivity" />
    </application>

    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


</manifest>